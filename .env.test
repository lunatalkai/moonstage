# vitest 專用（vitest 預設 mode=test，會載入這支）。
#
# src/config/env.js 在模組載入時就校驗必要變數，缺值直接拋錯——這是刻意的。
# 要保住這個「建構時就紅」的性質，測試環境就得自己提供配置，而不是放寬校驗。
#
# 用 .invalid 網域（RFC 2606 保留，保證不可解析），確保測試若不小心發出真實
# 請求會立刻失敗，而不是悄悄打到某個真的環境。

VITE_ENV_NAME=test
VITE_WS_BASE=wss://ws.test.invalid
