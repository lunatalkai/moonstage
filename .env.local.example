# cp .env.local.example .env.local
# .env.local is gitignored and applies to every mode, including production builds.

# Talk to a local API server instead of the public one
# DEV_PROXY_API=http://localhost:8888
# VITE_WS_BASE=ws://localhost:8888
